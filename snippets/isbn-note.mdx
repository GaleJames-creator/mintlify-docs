  <Note>In this example, the `isbn` field uses a mock format. In practice, you should use a valid ISBN for print books. The `publishedDate` field is also required for print books but not for ebooks.</Note>