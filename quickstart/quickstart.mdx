---
title: 'Quickstart-guide'
description: 'Start building the BookHub Publisher. See prerequisites and basic workflow to get started.'
---

## Prerequisites

1. BookHub publisher account
2. API credentials (JWT token)
3. HTTPS client (curl, Postman, or programming language HTTP library)

## Basic workflow

```bash
# 1. Create a book (returns PENDING status)
curl -X POST https://api.bookhub.com/api/v2/books \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "My New Book",
    "author": "John Doe",
    "description": "A new biography from a well-known author",
    "language": "en-US",
    "publisher": "My Publishing House",
    "publishedDate": "2024-03-15",
    "isbn": "978-0-1234-5678-9",
    "price": 24.99,
    "coverImages": ["https://example.com/cover.jpg"],
    "genres": ["Fiction", "Adventure"],
    "bookFormat": "Hardcover"
  }'

# 2. Get the book to see hitCount (starts at 0)
curl -X GET https://api.bookhub.com/api/v2/books/{bookId} \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"

# 3. List all books (sorted by creation date only)
curl -X GET https://api.bookhub.com/api/v2/books?sort=createdDate&order=desc \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"

# 4. Finalize the book (move from PENDING -> ACTIVE)
curl -X PATCH https://api.bookhub.com/api/v2/books/{bookId}/finalize \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"notes": "Ready for publication."}'
```

## Next steps

### Conceptual guides

* [Using `hitCount` for analytics](./conceptual-guides-hitcount-analytics) - Understand how to use the hitCount field to track book performance.

### How-to guides

* [Migrate BookHub guide from v1 to v2](/how-to-guides/how-to-guides-migrate-bookhub) - Comprehensive guide covering all changes in BookHub Publisher API v2, including the addition of the `hitCount` field and how to use it effectively.
* [Migrate away from title sorting](/how-to-guides/how-to-guides-migrate-away-from-title-sorting) - Guide to update your code to stop using title sorting in BookHub Publisher API v2, which is relevant if you want to sort books by popularity using `hitCount` instead.

### Reference

* [PHP example: Track high-performing books with hitCount](./reference/reference-code-examples#php-example-track-high-performing-books-with-hitcount) - Example code to use the hitCount field in PHP.
* [Release notes](./reference/reference-release-notes) - See all changes in v2, including the addition of the `hitCount` field and how it can be used for analytics.

---
Last updated: May 2026
