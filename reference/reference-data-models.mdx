---
title: "Data models"
description: 'Explore schema definitions for the BaseBook, BookWithAnalytics, Pagination, and Error response objects in the BookHub Publisher API.'
---

## BaseBook schema

The standard `book` object used in all endpoints. ISO 639-1 language code, optionally with a regional subtag (e.g., `en`, `en-US`).

```json
{
  "bookId": "string (read-only)",
  "title": "string (max 255 chars)",
  "author": "string (max 255 chars)",
  "description": "string (max 5000 chars)",
  "language": "string (ISO 639-1, e.g., en-US)",
  "publisher": "string (max 255 chars)",
  "publishedDate": "date (YYYY-MM-DD)",
  "isbn": "string (ISBN-10 or ISBN-13)",
  "price": "number (minimum 0.01)",
  "status": "enum (PENDING, ACTIVE, INACTIVE)",
  "coverImages": ["array of URLs (1-5 items)"],
  "genres": ["array of strings (1-5 items)"],
  "bookFormat": "enum (Paperback, Hardcover, eBook)",
  "createdDate": "datetime (ISO 8601, read-only)",
  "updatedDate": "datetime (ISO 8601, read-only)"
}
```

## BookWithAnalytics schema (v2 only)

This schema extends `BaseBook` with the following additional field:

```json
  "hitCount": "integer (minimum 0, read-only)"
```

## Pagination schema

The pagination object used in list endpoints (e.g., `GET /v2/books`) to provide pagination details.

```json
{
  "currentPage": "integer (minimum 1)",
  "totalPages": "integer (minimum 0)",
  "totalItems": "integer (minimum 0)",
  "itemsPerPage": "integer (1-100)"
}
```

## Error response schema

The error response object returned when an API request fails.

<Note>The `validation_context` and `provided_value` fields use snake_case rather than camelCase. This is a known inconsistency in the v2 API that will be addressed in a future version.</Note>

```json
{
  "status": "error",
  "message": "string",
  "errors": [
    {
      "field": "string",
      "error": "string",
      "description": "string",
      "validation_context": {
        "rule": "string",
        "constraint": "object",
        "provided_value": "string"
      }
    }
  ]
}
```

## Next steps

Explore related topics:

### Conceptual guides

* [Using the `hitCount` field for analytics](/conceptual-guides/conceptual-guides-hitcount-analytics) &mdash; Learn about the `hitCount` field in the BookHub Publisher API.

### How-to guides

* [Use `hitCount` to track book performance](/how-to-guides/how-to-guides-use-hitcount-to-track-book-performance) &mdash; Use the `hitCount` field to analyze book performance trends over time.

### Reference

* [Release notes](./reference/reference-release-notes) &mdash; Explore all changes in BookHub Publisher API v2.
* [API endpoints](/reference/reference-api-endpoints) &mdash; Explore all available API endpoints and their parameters.
* [Code examples](/reference/reference-code-examples) &mdash; Copy working code samples for book creation, sorting by creation date, handling removed title sorting, and tracking performance with `hitCount`.

---
